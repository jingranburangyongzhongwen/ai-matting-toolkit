# PPT 配图（与 `report/slide.md` 文首清单对齐）

把图片按下列文件名放入本目录即可（同名覆盖）。

| 文件名 | 页 |
|--------|-----|
| `research-boundary.png` | 调研 |
| `autoregressive-chain.png` | 模型 · 自回归 |
| `galton-baffle.png` | 模型 · 概率输出 |
| `structure-controllable.png` | 所以 · 结构让改动可控 |

建议：宽边 ≥ 1600px；JPG/PNG 均可（扩展名与上表不一致时，同步改 `slide.md` 路径）。
